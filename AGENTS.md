## Instruction Priority

2026-09-14 EXEC-167の利用者指示により、新規candidateの条件点は同じ分類内で原文の先頭に近い条件を重くする。集約profileはsource-order-linear-v1。入力位置の異なるN条件へN..1の重みを与え、同位置は同じ重みで正規化加重平均する。日英最大値・否定の負方向・分類間優先順・画像採点は維持する。重みをplan/履歴へ保存し、共通の結果/履歴内訳へ表示する。旧plan/履歴は再採点せず従来方式で読む。

2026-09-13 EXEC-162の利用者指示により、新規SigLIP 2画像評価は視覚条件の文章対商品画像と、参考/対比画像対商品画像の2経路へ分離する。順位は否定条件（負）、タイトル、優先、希望、文章対画像、画像対画像、レビューの順。同点時のみ次の項目を比較する。確認済み外観記述と既存商品画像特徴を使い、文章側に生成画像を混ぜない。旧profile/履歴を再採点せず、画像なしでは両経路を無効にする。結果/履歴の共通UIとモック・本番fixtureへ両点を反映する。

2026-09-13の利用者指示により、画像なし検索でも結果表示用の商品画像を取得し、192px以内のRGB PNGとして既存のprivate履歴へ保存する。参考画像・比較画像の生成と画像採点は無効のままにする。表示画像は既存の許可ホスト・安全な画像proxy経由で、最大24件、重複取得なし、失敗は画像欠損として検索を継続する。画像評価のprofile・日英採点・順位は変更せず、旧履歴を再取得・補完・再採点しない。後段の「画像なしでは商品画像を取得しない」という旧制約は、この表示用途に限り置き換える。

2026-09-13 EXEC-156では辞書の対比候補が0件の条件をBonsaiへ明示的に渡す。未知外観は一致側/対比側の記述、未知部品は英名を既存の視覚推論1回で補完する。辞書未収録だけを理由に保留しない。原文が解釈不能・不正応答の場合の文章修正、辞書優先、画像なしの推論省略、現行採点/履歴を維持する。

2026-09-13 EXEC-155では、接続画面の画像不使用を条件整理開始時から反映する。off時は原文の外観句をローカルで照合用に保持し、視覚Bonsai・対比辞書・対比確定を省く。画像なし準備のplanはtext-only-v1で区別し、画像生成/画像採点へ転用しない。画像使用への切り替えは既存3回上限内で再整理する。商品名展開・日英採点・最終確認・既存履歴は維持する。

2026-09-13 EXEC-154では部品付き条件の対比準備を修正する。新規v3は「〜付き」等を部品有無の文法として扱い、既存WordNet/JMdictの英名候補を参照する。複数語義は既存Bonsai視覚要求の候補ID選択、未知部品は短い英名提案だけを使い、有無の指示はコードで構築する。独自部品辞書、追加Bonsai要求、商品名の新分解、採点方式変更は行わない。旧v1/v2と保存履歴を保持する。

2026-09-13 EXEC-153の利用者指示により、新規対比指示は独自対応表を使わず、既存日本語WordNetとPrinceton WordNet 3.0の直接反対語を優先する。単一語義はコードで確定し、複数語義は定義と候補を既存Bonsai視覚要求に渡す。部品有無は辞書英訳と文法で反転し、曖昧な文法否定は補助情報だけに使う。旧手書き表はv1計画の読取互換だけに残す。v2の出典/語義/digestを保持し、商品名・条件分類・採点を変更しない。追加推論/画像生成/本番検索は実施しない。

2026-09-13 EXEC-152の利用者指示により、対比画像用の具体的な外観変更はローカル変換を優先し、未対応条件だけ既存Bonsai視覚条件要求の応答で補完する。これは画像生成用の一致側/不一致側の記述に限定し、条件分類・検索語・商品採点へ転用しない。追加Bonsai呼出しは行わず、準備あたり最大2回を維持する。否定条件は参考側/対比側を反転する。新しいcontrast fieldとprompt-v3 digestを旧計画/旧画像要求と区別し、既存履歴を再生成・再採点しない。今回の検証に追加の実モデル実行・画像生成・検索・課金は含めない。

2026-09-12 EXEC-145の利用者指示で、接続画面をオフラインモックの共通ヘッダー・段階表示・画像/調査状態・結果カード・履歴・ダイアログ操作へ統一する。自然文による条件修正と現行順位/履歴を保持する。新しく検索、条件へ戻る、2回上限の画像作り直し、保存だけの再試行、安全な取得後の中止をserver状態へ接続する。実行中に新しいworkerを作らず、中止は進行中通信の即時取消とは扱わない。起動済みserverの新しい検索は作業先を分離し、準備は各検索3回、履歴は起動時の共通DBへ保存する。実サービスの追加試験/課金は含めない。

2026-09-12 EXEC-144の利用者指示で、新しい接続画面の完了履歴へ元入力全文（最大2000文字）、確定した条件名/分類と原文範囲、取得済みの商品サムネイル（192px以内のRGB PNG）をprivate SQLiteに保存する。利用者データの保存許可はこのアプリ履歴に限定し、ログ/文書/review artifact/commitへの出力禁止を維持する。schema5の任意history-content-v1を使い、旧履歴を補完/再採点しない。入力/ラベル/PNGは履歴と同じtransaction/digest/30日期限/削除対象に含める。画像なしでは画像取得を追加しない。

2026-09-12 EXEC-143の利用者指示で、履歴一覧/詳細の確認付き個別削除と30日期限削除を接続する。GETはread-onlyを維持し、同一originのPOSTとserver稼働中の60秒周期cleanupだけが起動時指定済みの既存schema5 DBへ書く。対象ownerはlocal-user固定、時刻はserver側。既存file限定のmode=rwを使い、未作成run DBや旧schemaを作成/移行しない。履歴と専有画像を同一DB transactionで削除し、失敗は表示/再試行する。削除の試験はfixtureだけで実施し、実利用者の有効な履歴をテストとして削除しない。新規検索/provider/課金や生cache削除は含めない。

2026-09-12 EXEC-142の利用者指示で、接続画面の画像なし検索をcandidate→Playwright→日英最大値採点→SQLite履歴まで接続する。検索語確認/画像確認/最終確認、画像準備失敗と画像承認期限切れから明示的に切り替え、商品取得前の最終確認を維持する。外観条件0件では画像生成を許さず画像なしで続行できる。画像なしはCloudflare設定・画像生成・画像proxy・SigLIPを呼ばず、新しいcandidate-image-free-v1/image-free-v1 profileとimage_mode=offで保存する。既存画像profile/履歴/承認を読み替えない。入力2000文字、準備3回、Bonsai最大2回/準備、商品24件、worker1は維持し、追加の本番検索/課金は今回の検証に含めない。

このリポジトリでは、以下の順序で指示を適用する。

1. `AGENTS.md`
2. `docs/DEVELOPMENT.md`
3. その他のプロジェクト文書
4. 既存コードから推測される慣習

`AGENTS.md` と `docs/DEVELOPMENT.md` が競合する場合は、`AGENTS.md` を優先する。
`docs/DEVELOPMENT.md` とその他のプロジェクト文書が競合する場合は、`docs/DEVELOPMENT.md` を優先する。

実装、修正、リファクタリング、テスト、コードレビュー、コミットメッセージ作成を行う前に、`docs/DEVELOPMENT.md` を読み、そのルールに従う。
`docs/DEVELOPMENT.md` のルールを、既存コードの慣習だけを理由に無視してはならない。

2026-09-12 EXEC-133の利用者指示により、新規candidateは条件の同義語・英訳をローカル辞書/OPUS-MTで取得し、日英の商品タイトルと詳細を採点して各条件の高い一致点を使う。否定条件の最大一致は除外側に働く。画像点との独立性を維持し、最終順位は後続のEXEC-139に従う。旧履歴は旧方式で読み、商品採点LLMを追加しない。詳細はdocs/BACKEND.mdのEXEC-133を参照する。

2026-09-12 EXEC-134の接続指示により、OPUS-MT設定時は辞書候補ありからの未収録商品名提案にも名前専用Bonsai要求を使い、英訳はローカル翻訳へ渡す。辞書選択/再照会で確定した英訳を優先し、未収録提案は原語と提案名を重複なしで翻訳する。Bonsai応答のenglishはnullだけを許容する。翻訳失敗で日本語候補を失わず、Bonsai英訳へ戻さない。視覚条件のBonsai用途と、OPUS-MT未設定の旧経路は維持する。

## 認証情報とコマンド出力

2026-09-12 EXEC-138の接続継続指示により、明示指定したschema5 SQLiteの永続履歴一覧/詳細をloopback APIとReactへ接続する。既存DBはread-onlyで開き、ownerはserver側local-user固定。起動時指定DB最大29個と新規run保存先3個、最新30履歴を扱い、HTTPからDBパスを受け付けない。履歴だけの起動/閲覧/再読込ではproviderを起動せず、保存済み点数/生成画像を復元する。未保存の商品画像や元入力を再取得/推測で補わない。削除と期限切れの物理削除は後続のEXEC-143で接続済み。後段の永続履歴未接続の記述はこの一覧/詳細範囲では更新済みとする。

2026-09-12 EXEC-137の接続継続指示により、接続画面は2000文字以内の自然文入力と画像生成前の文章修正を受け付ける。初回を含め準備3回、各Bonsai最大2回、視覚条件1〜3件・商品24件の画像あり経路を接続する。取得済み画像の縮小表示と日英採点内訳を追加し、ブラウザから商品画像を再取得しない。旧固定入力制限はこの範囲で解除する。画像なし・構造化条件の個別編集・永続履歴一覧・GPU切替は未接続であり、任意入力の品質合格は主張しない。

- トークン、PAT、API キー、パスワード、秘密鍵などの機密値を、コマンドライン引数、ログ、標準出力、標準エラー、コミット、Pull Request、またはユーザーへの回答へ露出させない。
- `gh auth token` のように機密値そのものを取得・表示するコマンドは、その値が作業に不可欠でない限り実行しない。
- `--token="$(...)"` のように、機密値をコマンドライン引数へ展開して渡さない。実行ツール、パッケージマネージャー、ラッパー、またはプロセス一覧が引数を記録・表示する可能性がある。
- 認証済み CLI の資格情報ストアや CI の Secret 機構など、機密値をコマンドラインへ展開しない認証方法を優先する。利用するツールが機密値をログ出力しないことも事前に確認する。
- 機密情報を扱う処理では、`set -x`、verbose/debug ログ、dry-run、notice などがコマンドや環境変数を出力しないか、実行前に確認する。安全な受け渡し方法を確認できない場合は実行せず、ユーザーへ報告する。
- 機密値が出力された場合は再掲せず、当該認証情報を漏えい済みとして扱う。関連する外部書き込みを停止し、失効・ローテーションと影響確認をユーザーへ案内する。

## amazon-explorer 固有の安全境界

- 2026-09-12 EXEC-139の利用者指示以降、新規candidateの最終順位は否定条件（負）＞タイトル一致＞優先条件（required）＞希望条件（preferred）＞画像評価＞レビュースコア。否定一致は小さい方、他は大きい方を先にし、同点時だけ次を比較する。レビューは取得済みAmazon星評価0〜5、未取得は評価済み0より後。全項目同点なら取得順。旧title-image-conditions-v1とfieldなしの履歴は保存当時の方式で読む。新sort_profile_idはexcluded-title-conditions-image-review-v1。日英最大値と画像の独立性を維持し、商品採点LLMを追加しない。50:50合成点は参考値で順位keyに使わない。

- 2026-09-12 EXEC-126の利用者の明示決定により、現行の商品検索・詳細取得をPlaywrightへ移行する。ブラウザ接続、CLI、candidate実行/再実行は匿名のローカルChromiumを使い、Outscraper API keyや有料providerへの自動fallbackを必要としない。今回の移行・検証に追加承認は不要。既存の検索語/画像確認UIは維持する。旧要求・履歴の読込互換と旧診断protocolのadapterを残し、新要求digestとplaywright-v1 cacheを分離する。実行境界と現行仕様はdocs/BACKEND.mdのEXEC-126を正本とし、後段のOutscraper記述は旧契約として扱う。

- `.env`、`.streamlit/secrets.toml`、live API の要求・応答、利用者の検索入力、生キャッシュに含まれる機密情報または利用者データを、ログ、文書、review packet、artifact、コミットへ含めない。`.env.example` には実値を入れず、プレースホルダーだけを管理する。
- 通常の検証は事前準備済み環境で `uv run --frozen --offline --no-sync pytest -m 'not live_api'` を使う。`uv sync` は依存取得の通信を行い得るため別の環境準備として扱う。`live_api` marker や `--run-live-api` は技術的な opt-in であり、credential 利用、外部送信、課金の承認ではない。各 live 実行の直前に、送信先、要求範囲、回数・上限、credential 利用の有無、想定費用を示し、人間の明示承認を得る。
- mock、fixture、静的解析、ローカル subprocess、offline test、`nonlive_ready` を、Bonsai、Outscraper、OpenAI、Cloudflare の実サービス成功または production E2E と表現しない。
- 商品検索 LLM は現行のローカル Bonsai 経路を正本とし、OpenAI へ自動 fallback しない。candidate 経路の Bonsai 用途は CLIP 用視覚条件と、文脈に基づく商品名候補の選択・補完に限定する。検索語は任意のローカル辞書・構文解析・語義選択を使う。辞書未設定/保留時は元 query を保持する。明示的に構成した商品名補完経路では、未収録の候補を未確認として返し、人間確認前に検索へ適用しない。`src/search_v2/` は strict intent、要求・応答、正規化、画像、ランキングを含むdomain基盤であり、内部のoffline orchestrationではtyped proposal付き第1確認から `typed-ranking-v4` と表示履歴までを接続している。ただしlegacy現行パイプライン、実provider、API、UIへは未接続である。provider 変更には明示決定、互換 adapter、回帰テスト、cache 移行方針を必要とする。
- 2026-09-10の決定以降、candidate経路のBonsaiはCLIP用の視覚条件提案と、商品名候補の辞書語義ID選択・未収録名の提案だけに使う。英訳・言い換えは辞書を優先し、未収録時はBonsaiの提案と出典を分け、人間が確認した候補だけ検索へ使う。 辞書照会が正常に候補0件を返した場合は、原文だけに基づくBonsaiの商品名・英訳推論へ分岐し、生成後に辞書へ再照会しない。辞書未設定・照会失敗はこの分岐に含めない。2026-09-10の接続指示により、OPUS-MT設定時はこの分岐のBonsaiを日本語名提案だけにし、原語と提案名の英訳各1件をローカルOPUS-MTで作る。同じ句は1回だけ翻訳し、翻訳失敗では英訳をnullにして日本語候補を保持する。未設定時の従来経路と登録済み辞書の英訳を維持する。商品採点・商品本文の属性追加推論には使わず、SudachiPyと既存規則、観測値の条件判定、タイトル単語一致とCLIPで順位を決める。旧意味評価の診断やlegacy実装を根拠に、この経路へ商品評価LLMを再接続しない。
- `src/search_v2/holdout_evaluation.py` の合成fixture成功は評価契約と計算の確認であり、実Bonsaiの条件分解、実商品の属性充足、未知データの順位品質を証明しない。case1からcase3をdevelopment setとして除外し、実holdoutを開く前に用途別の品質合格基準を固定する。予測artifactへ検索文、商品本文、ASIN、生provider応答、生例外を保存しない。
- 2026-09-11の利用者の明示決定により、次期フロントエンドはこのリポジトリで自前開発する。外部納品を着手条件とする制約を解除し、`SEARCH-FLOW.md` と `docs/FRONTEND.md` を自前開発の目標仕様とする。文書の正本化に続く利用者の実装指示により、`frontend/` に画面とオフラインモックを実装した。自前開発では納品完了の確認を必要としない。実装はReact + StyleX、Figmaはレイアウトの参考とし、色・文字・装飾・操作配置は `docs/FRONTEND.md` の次期UI視覚統一契約を正本とする。任意の自然言語入力から固定の合成結果まで操作できるオフラインモックを維持し、実バックエンド・Bonsai・外部API・credentialを使わず、実サービスへ自動接続しない。React画面・合成データ・ローカル資材とタブ内履歴を実装し、検証結果は `docs/GOAL.md` のEXEC-117へ記録する。実バックエンド/API接続と本番の永続履歴は別の後続作業とする。ローカル専用・単一host・単一process・worker 1本の検索実行構成と、実サービス利用・外部送信・credential・課金の承認規則は維持する。
- 2026-09-11の利用者指示によりStreamlitを削除した。画面はReact + StyleXのオフラインモック、実検索はCLIを入口とする。Reactと実バックエンド/APIの接続は未実装である。旧 `.streamlit/secrets.toml` の漏えい防止規則は維持する。
- AI レビューの attested `pass` も、commit、push、merge、外部送信、credential 利用、課金を許可しない。これらはそれぞれ人間の明示的な権限を必要とする。
- AI レビューハーネスを操作する前に [`docs/HARNESS-RUNBOOK.md`](docs/HARNESS-RUNBOOK.md) を読む。同ファイルを実行コマンド、期待結果、停止・再実行規則の正本とし、CLI、policy、信頼境界を変更したときは関連文書と同時に更新する。

# Markdown目次

2026-09-12 EXEC-125の利用者指示により、新規candidateの条件採点へ説明文の部分一致を追加する。構造化された観測を優先し、否定・数値/単位・欠損を扱う。同義語は元語優先のタイトル補助点とし、日英カテゴリ・明示されたブランド/型番を専用fieldへ固定する。仕様の確認状態と文章の一致度を混同せず、商品採点LLMは追加しない。旧保存結果は再採点せず、新text profile/digestで区別する。詳細はdocs/BACKEND.mdとdocs/GOAL.mdのEXEC-125を参照する。

## 入口

- [`README.md`](README.md)
    - リポジトリの概要、インストール方法、使い方を説明する。
- [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md)
    - リポジトリの開発ルールを決定づける。

## 参照

- [`docs/REQUIREMENTS.md`](docs/REQUIREMENTS.md)
    - プロダクト、実行環境、依存関係、受入条件を定義する。
- [`docs/FRONTEND.md`](docs/FRONTEND.md)
    - フロントエンド設計を定義する。
- [`docs/BACKEND.md`](docs/BACKEND.md)
    - バックエンド設計を定義する。
- [`docs/SECURITY.md`](docs/SECURITY.md)
    - セキュリティ設計を定義する。
- [`docs/DB-SCHEMA.md`](docs/DB-SCHEMA.md)
    - データ保存と論理スキーマを定義する。
- [`docs/REFERENCES.md`](docs/REFERENCES.md)
    - 開発時に参照した資料を管理する。

## 作業管理

- [`NEXT-STEPS.md`](NEXT-STEPS.md)
    - 残作業と、利用者側で用意するものを簡潔に示す。
- [`docs/ISSUES.md`](docs/ISSUES.md)
    - 問題と作業台帳への入口を管理する。
- [`docs/GOAL.md`](docs/GOAL.md)
    - プロダクトのゴールと進行中の計画を定める。
- [`docs/WORKLOG.md`](docs/WORKLOG.md)
    - 検証境界を含む開発履歴を記録する。
- [`docs/CHANGELOG.md`](docs/CHANGELOG.md)
    - 利用者・運用者に影響する統合済み変更を記録する。

## プロジェクト固有の正本

- [`SEARCH-FLOW.md`](SEARCH-FLOW.md)
    - 次期商品検索を利用する人の画面操作と確認・回復フローを定義する。
- [`docs/HARNESS-RUNBOOK.md`](docs/HARNESS-RUNBOOK.md)
    - AIレビューハーネスの実行コマンド、期待結果、停止・再実行規則を定義する。

2026-09-13の利用者指示により、統合済み旧文書は `bin/docs/old/`、未使用の旧段階サンプルは `bin/examples/legacy-phases/` にローカル退避し、`bin/` 全体をGit追跡外とする。現行文書は統合先を参照し、退避物に依存しない。アーカイブは現行の要件、設計、手順、進行状態を上書きしない。


2026-09-11 EXEC-116の明示決定以降、candidateの画像比較既定はSigLIP 2参考画像方式（siglip2_appearance）。Bonsaiの用途制限は維持し、商品順位用LLMを追加しない。旧CLIPは明示選択と旧履歴互換に限る。SigLIP資材/別Python環境を事前準備し、新profile/runtimeで旧cacheと分離する。フロントエンドは上記の自前開発方針に従い、外部実行の境界は維持する。

2026-09-12 EXEC-124では、利用者のフロントエンド接続・本番テスト指示により、固定マグカップ入力を対象とするローカル接続試験入口を追加した。既定モックを維持し、明示起動した `tools.browser_search_server` と `?mode=connected` だけをcandidate/SigLIP 2へ接続する。画像あり・1検索の範囲で検索語、参考画像、比較画像、検索の確認を維持する。実サービス実行の個別承認規則は変更しない。任意入力・画像なし・条件編集・永続履歴一覧の実接続は後続。詳細と現在の検証状態はdocs/GOAL.mdのEXEC-124を参照する。

2026-09-12の利用者指示により、EXEC-124のブラウザ接続試験は全体15分制限を設けない。個々の通信/推論のtimeout、呼出回数上限、比較画像生成後のsingle-use承認期限と人間確認は維持する。

2026-09-12 EXEC-140の承認済み計画により、candidate条件の自然文分類はcondition-language-v1の共通ローカル解析を使う。希望と否定の重なりは否定優先、希望付き価格は希望、指定なしは原文/確認表示に残して検索語/採点/画像から外す。仕様の非対応falseを一律除外へ変更しない。Bonsaiは確定した分類に従って外観候補を選び、分類用LLM呼出しを増やさない。曖昧/矛盾は既存の文章修正操作へ戻す。旧計画/履歴を新解釈で再採点しない。追加本番検索/課金と画像なし接続は今回の範囲外。詳細はdocs/GOAL.mdのEXEC-140。
